/* The degenerate cycle: an emitter reaching itself. Same reason, same report. */
// EXPECT_ERROR: emitter expansion cycle
#beguilerSettings { target = z5; informName = "none"; outputPath = "tests/output"; }
emitter int loopy(int v){ $i6Expr(loopy($v)) }
void main(){ int r = loopy(1); print(r); }

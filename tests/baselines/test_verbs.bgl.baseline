!% -G
!% -E1
Constant beguiler = 1000;
#Ifdef beguiler;#Endif;

    Constant story    = "";
    Constant headline = "";


    replace main _oldmain;

global _bglInitDone = 0;
[bglInit;
    if(_bglInitDone) return;
    _bglInitDone = 1;
];
object _bglUtil
;
object bgl
  with utilities _bglutil
;
object whiff
  with handler [;
    print (string)"You take a whiff of the air.";
  ]
;
[whiffsub;
                whiff.handler();
            ];
verb 'whiff'
    * -> whiff;
verb 'waft'
    * noun -> whiff;
object nudge
  with handler [;
    print (string)"You nudge it.";
  ]
;
[nudgesub;
                nudge.handler();
            ];
verb 'nudge'
    * noun -> nudge;
verb 'jostle'
    * noun -> nudge;
[handleAction a;
    if (a == ##whiff) {
        print (string)"whiffing";
    }
    else {
        if (a == ##nudge) {
            print (string)"nudging";
        }
        else {
            if (a == ##examine) {
                print (string)"examining";
            }
            else {
                print (string)"other";
            }
        }
    }
];
[testVerbs;
    handleaction(##whiff);
    handleaction(##examine);
];
object possess
  with handler [;
    print (string)"Possessed.";
  ]
;
[possesssub;
                possess.handler();
            ];
verb 'carol^s'
    * noun -> possess
    * noun -> possess;
object greet
  with handler [;
    print (string)"Greeted.";
  ]
;
[greetsub;
                greet.handler();
            ];
verb 'mr'
    * noun -> greet;
object stow
  with handler [;
    print (string)"Stowed.";
  ]
;
[stowsub;
                stow.handler();
            ];
verb 'stow'
    * held 'on'/'onto'/'upon' noun -> stow
    * held 'in'/'into'/'inside' noun -> stow;
object testObj
  with score 0,
       bump [;
    self.score=self.score + 1;
  ]
;
[testMemberAccess s;
    testobj.bump();
    s=testobj.score;
];

    [main; 
        bglInit();
        _oldmain();
    ];

